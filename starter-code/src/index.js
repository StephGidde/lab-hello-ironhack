// works like require in the backend
import React from "react";
import ReactDOM from "react-dom";

//App is a React component
import App from "./App.js";

ReactDOM.render(
  // render our component into DOM element
  <App />,
  document.getElementById("root")
);
